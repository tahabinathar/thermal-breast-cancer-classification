# EXP02_DMR_Anterior_ResNet18

## Run 05: OOF Threshold Selection

### 1. Objective

The objective of Run 05 was to select the final classification threshold for the EXP02 ResNet-18 model using the complete out-of-fold (OOF) prediction set generated in Run 04.

Run 04 generated OOF probabilities for all 197 images in the development dataset using the fixed configuration selected in the preceding runs:

* Image size = 224 × 224
* Learning rate = 1e-3
* Dropout rate = 0.3
* Fixed training epoch = 32
* ResNet-18 ImageNet-pretrained backbone
* Frozen backbone with a trainable classification head

Run 05 evaluated the complete set of OOF prediction probabilities generated by the five-fold cross-validation procedure.

A threshold was considered valid only if it satisfied both of the following conditions:

* Sensitivity > 0.91
* Specificity > 0.60

Among all valid thresholds, the selection criterion was highest sensitivity. If sensitivity was tied, the threshold with the highest specificity was selected. If both sensitivity and specificity were tied, the higher threshold was selected as the final deterministic tie-breaker.

The test set was not used in Run 05.

---

### 2. Methodology

```text
RUN 05

│
├── Input
│   └── Run 04 OOF predictions
│
├── Development Dataset
│   ├── Total = 197 images
│   ├── Healthy = 125
│   ├── Sick = 72
│   └── Test = NOT USED
│
├── OOF Predictions
│   ├── Fold 1
│   ├── Fold 2
│   ├── Fold 3
│   ├── Fold 4
│   └── Fold 5
│
├── OOF Summary
│   ├── ROC-AUC
│   └── PR-AUC
│
├── Threshold Evaluation
│   └── Every unique OOF probability
│
├── Constraint Filtering
│   ├── Sensitivity > 0.91
│   └── Specificity > 0.60
│
├── Threshold Selection
│   ├── Highest sensitivity
│   ├── Tie-breaker = highest specificity
│   └── Final tie-breaker = highest threshold
│
└── Selected Threshold
    └── 0.192174
```

The OOF probabilities from Run 04 were treated as continuous prediction scores. Each candidate threshold was applied to all 197 OOF probabilities to generate binary predictions.

For each threshold, the following metrics were calculated:

* Sensitivity
* Specificity
* Accuracy
* Balanced accuracy
* Precision
* F1-score
* Confusion matrix components: TN, FP, FN, TP

The threshold selection was performed entirely on the OOF predictions. No model retraining was performed in Run 05.

---

### 3. Configuration

| Parameter            | Value                        |
| -------------------- | ---------------------------- |
| Input                | Run 04 OOF predictions       |
| Development dataset  | 197 images                   |
| Healthy              | 125                          |
| Sick                 | 72                           |
| OOF folds            | 5                            |
| Threshold evaluation | Every unique OOF probability |
| Minimum sensitivity  | > 0.91                       |
| Minimum specificity  | > 0.60                       |
| Selection criterion  | Highest sensitivity          |
| Tie-breaker          | Highest specificity          |
| Final tie-breaker    | Highest threshold            |
| Test set             | Not used                     |

The overall OOF ROC-AUC and PR-AUC of the prediction set were calculated before threshold selection.

---

### 4. Threshold Evaluation and Selection

The complete OOF prediction set contained 197 predictions:

* Healthy = 125
* Sick = 72
* Fold assignments = 1–5
* Missing OOF predictions = 0

The overall OOF discrimination metrics were:

**Overall OOF ROC-AUC = 0.892667**

**Overall OOF PR-AUC = 0.832326**

Thresholds were evaluated using every unique OOF probability generated by Run 04.

A threshold was retained as valid only when:

```text
Sensitivity > 0.91

AND

Specificity > 0.60
```

A total of **5 thresholds** satisfied both constraints.

The final threshold was selected using the following hierarchy:

```text
1. Highest sensitivity

2. Highest specificity if sensitivity was tied

3. Highest threshold if both were tied
```

The selected threshold was:

**0.192174**

---

### 5. OOF Prediction Evaluation

The selected threshold produced the following results on the complete 197-image OOF prediction set:

| Metric            |    Value |
| ----------------- | -------: |
| Threshold         | 0.192174 |
| Sensitivity       |   0.9306 |
| Specificity       |   0.6320 |
| Accuracy          |   0.7411 |
| Balanced accuracy |   0.7813 |
| Precision         |   0.5929 |
| F1-score          |   0.7243 |
| TN                |       79 |
| FP                |       46 |
| FN                |        5 |
| TP                |       67 |

The confusion matrix was therefore:

```text
                 Predicted

              Healthy    Sick

Actual Healthy    79       46

Actual Sick        5       67
```

The selected threshold produced a sensitivity of 0.9306 and specificity of 0.6320, satisfying both predefined threshold constraints.

---

### 6. Selection Rule

The threshold selection was constraint-based rather than based on maximum accuracy, F1-score, ROC-AUC, or PR-AUC.

The implemented selection rule was:

```python
selected_result = max(
    valid_results,
    key=lambda x: (
        x["sensitivity"],
        x["specificity"],
        x["threshold"],
    ),
)
```

Therefore, the threshold was selected according to the following priority:

```text
Highest sensitivity

        ↓

Highest specificity

        ↓

Highest threshold
```

The OOF ROC-AUC and PR-AUC were used to describe the overall discrimination of the model predictions, but neither was used to select the classification threshold.

---

### 7. Results

#### OOF Prediction Summary

| Parameter            |   Result |
| -------------------- | -------: |
| OOF predictions used |      197 |
| Healthy              |      125 |
| Sick                 |       72 |
| Number of folds      |        5 |
| Overall OOF ROC-AUC  | 0.892667 |
| Overall OOF PR-AUC   | 0.832326 |
| Valid thresholds     |        5 |
| Test set used        |       No |

#### Selected Threshold Results

| Metric             |   Result |
| ------------------ | -------: |
| Selected threshold | 0.192174 |
| Sensitivity        |   0.9306 |
| Specificity        |   0.6320 |
| Accuracy           |   0.7411 |
| Balanced accuracy  |   0.7813 |
| Precision          |   0.5929 |
| F1-score           |   0.7243 |
| TN                 |       79 |
| FP                 |       46 |
| FN                 |        5 |
| TP                 |       67 |

The selected threshold satisfies:

```text
Sensitivity = 0.9306 > 0.91

Specificity = 0.6320 > 0.60
```

No test-set predictions were used for threshold selection.

---

### 8. Final Run 05 Decision

The final threshold selected from the complete OOF prediction set was **0.192174**.

| Parameter            | Value                        |
| -------------------- | ---------------------------- |
| OOF source           | Run 04                       |
| Development dataset  | 197 images                   |
| OOF predictions      | 197 / 197                    |
| Overall OOF ROC-AUC  | 0.892667                     |
| Overall OOF PR-AUC   | 0.832326                     |
| Threshold evaluation | Every unique OOF probability |
| Minimum sensitivity  | > 0.91                       |
| Minimum specificity  | > 0.60                       |
| Valid thresholds     | 5                            |
| Selected threshold   | **0.192174**                 |
| Sensitivity          | **0.9306**                   |
| Specificity          | **0.6320**                   |
| Accuracy             | 0.7411                       |
| Balanced accuracy    | 0.7813                       |
| Precision            | 0.5929                       |
| F1-score             | 0.7243                       |
| Test set             | Not used                     |

The threshold of **0.192174** will be carried forward as the fixed classification threshold for the final model evaluation on the untouched test set.

---

### 9. Output Structure

```text
Run_05_OOF_Threshold_Selection/

├── README.md

├── results/

│   ├── threshold_analysis.csv
│   ├── selected_threshold.json
│   └── overall_summary.json

└── plots/

    ├── sensitivity_specificity_vs_threshold.png
    └── valid_thresholds.png
```

#### `threshold_analysis.csv`

Contains the evaluation results for all unique OOF probabilities, including:

* Threshold
* Sensitivity
* Specificity
* Accuracy
* Balanced accuracy
* Precision
* F1-score
* TN
* FP
* FN
* TP
* Whether the threshold satisfies both constraints

#### `selected_threshold.json`

Stores the selected threshold, selection method, sensitivity and specificity constraints, performance metrics, confusion matrix values, number of OOF predictions, overall OOF ROC-AUC and PR-AUC, and confirmation that the test set was not used.

#### `overall_summary.json`

Stores the overall Run 05 OOF summary, including the development-set composition, number of folds, overall OOF ROC-AUC, overall OOF PR-AUC, threshold constraints, number of valid thresholds, selected threshold, and test-set usage status.

#### Plots

Two plots were generated:

1. **Sensitivity and specificity vs classification threshold**

2. **Thresholds satisfying sensitivity and specificity constraints**

---

### 10. Final Summary

* OOF source: Run 04
* Development images: 197
* Healthy images: 125
* Sick images: 72
* OOF folds: 5
* Overall OOF ROC-AUC: **0.892667**
* Overall OOF PR-AUC: **0.832326**
* Threshold evaluation: Every unique OOF probability
* Minimum sensitivity: > 0.91
* Minimum specificity: > 0.60
* Valid thresholds: **5**
* Selected threshold: **0.192174**
* Sensitivity: **0.9306**
* Specificity: **0.6320**
* Accuracy: **0.7411**
* Balanced accuracy: **0.7813**
* Precision: **0.5929**
* F1-score: **0.7243**
* TN: 79
* FP: 46
* FN: 5
* TP: 67
* Test set: **Not used**

Run 05 successfully selected a fixed classification threshold of **0.192174** from the complete 197-image OOF prediction set generated in Run 04. The selected threshold satisfies the predefined sensitivity and specificity constraints and will be used in the final evaluation on the untouched test set.